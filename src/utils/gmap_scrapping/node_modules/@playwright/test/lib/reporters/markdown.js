"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _fs = _interopRequireDefault(require("fs"));
var _path = _interopRequireDefault(require("path"));
var _base = require("./base");
var _util = require("../util");
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }
/**
 * Copyright (c) Microsoft Corporation.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

class MarkdownReporter extends _base.BaseReporter {
  constructor(options) {
    super();
    this._options = void 0;
    this._options = options;
  }
  printsToStdio() {
    return false;
  }
  async onEnd(result) {
    await super.onEnd(result);
    const summary = this.generateSummary();
    const lines = [];
    if (summary.fatalErrors.length) lines.push(`**${summary.fatalErrors.length} fatal errors, not part of any test**`);
    if (summary.unexpected.length) {
      lines.push(`**${summary.unexpected.length} failed**`);
      this._printTestList(':x:', summary.unexpected, lines);
    }
    if (summary.flaky.length) {
      lines.push(`**${summary.flaky.length} flaky**`);
      this._printTestList(':warning:', summary.flaky, lines);
    }
    if (summary.interrupted.length) {
      lines.push(`**${summary.interrupted.length} interrupted**`);
      this._printTestList(':warning:', summary.interrupted, lines);
    }
    const skipped = summary.skipped ? `, ${summary.skipped} skipped` : '';
    lines.push(`**${summary.expected} passed${skipped}**`);
    lines.push(`:heavy_check_mark::heavy_check_mark::heavy_check_mark:`);
    lines.push(``);
    if (summary.unexpected.length || summary.fatalErrors.length) {
      lines.push(`<details>`);
      lines.push(``);
      if (summary.fatalErrors.length) this._printFatalErrorDetails(summary.fatalErrors, lines);
      if (summary.unexpected.length) this._printTestListDetails(':x:', summary.unexpected, lines);
      lines.push(`</details>`);
    }
    const reportFile = (0, _util.resolveReporterOutputPath)('report.md', this._options.configDir, this._options.outputFile);
    await _fs.default.promises.mkdir(_path.default.dirname(reportFile), {
      recursive: true
    });
    await _fs.default.promises.writeFile(reportFile, lines.join('\n'));
  }
  _printTestList(prefix, tests, lines) {
    for (const test of tests) lines.push(`${prefix} ${(0, _base.formatTestTitle)(this.config, test)}`);
    lines.push(``);
  }
  _printTestListDetails(prefix, tests, lines) {
    for (const test of tests) this._printTestDetails(prefix, test, lines);
  }
  _printTestDetails(prefix, test, lines) {
    lines.push(`${prefix} <b> ${(0, _base.formatTestTitle)(this.config, test)} </b>`);
    let retry = 0;
    for (const result of test.results) {
      if (result.status === 'passed') break;
      if (retry) lines.push(`<b>Retry ${retry}:</b>`);
      retry++;
      if (result.error) {
        lines.push(``);
        lines.push('```');
        lines.push((0, _base.stripAnsiEscapes)((0, _base.formatError)(result.error, false).message));
        lines.push('```');
        lines.push(``);
      }
    }
    lines.push(``);
  }
  _printFatalErrorDetails(errors, lines) {
    for (const error of errors) {
      lines.push(`:x: <b>fatal error, not part of any test</b>`);
      lines.push(``);
      lines.push('```');
      lines.push((0, _base.stripAnsiEscapes)((0, _base.formatError)(error, false).message));
      lines.push('```');
      lines.push(``);
    }
    lines.push(``);
  }
}
var _default = MarkdownReporter;
exports.default = _default;